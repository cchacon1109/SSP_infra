name: Deploy to aws

on:
  push:
    branches: [develop]

jobs:
  build:
    runs-on: ubuntu-latest  
    permissions:
      id-token: write
      contents: read
    env:
      ENVIRONMENT: SSP_DEV
      AWS_REGION: us-east-2
      AWS_ACCOUNT_ID: 987206564851
      SUPABASE_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imt0eGhkaWZvam1sbHRlb3d6d3N6Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3MTM4NDAxMDQsImV4cCI6MjAyOTQxNjEwNH0.sfACZieKDEXPsZcxZt3ov2FEMm46hlmFLPU7SbfA8Zs"
      SUPABASE_JWT_SECRET: "5YMVe//fZMaaZ5jIlzTcucrsSaieEzljeoDo0aqEYhP0IuF/6tGpT5mYtNZoi20Sf8S3F78OkYVyk8ePs//KTQ=="
      SUPABASE_URL: "https://ktxhdifojmllteowzwsz.supabase.co"
    steps:
      
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Prerequisite Installation
        run: |
          sudo npm install -g aws-cdk
          cdk --version

      - name: Install dependencies
        run: |
          cd deploy
          npm install
      
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: us-east-2
          role-to-assume: arn:aws:iam::987206564851:role/github-oidc-cdk-ssp

      - name: Deploy SSPApigatewayStack
        run: |
          cd deploy
          cdk deploy --require-approval never SSPApigatewayStack --debug --trace

      - name: Deploy SSPSupabaseSecretStack
        run: |
          cd deploy
          cdk deploy --require-approval never SSPSupabaseSecretStack --debug --trace
          
      - name: Deploy SSPRolesStack
        run: |
          cd deploy
          cdk deploy --require-approval never SSPRolesStack --debug --trace